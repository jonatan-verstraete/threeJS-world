import { Vector } from "../World";

export type FontOptions ={
    font?: string,
    fontSize?: number,
    color?: string,
    maxWidth?: number,
    lineHeight?: number,
    letterSpacing?: number,
    textAlign?: string,
    materialType?: string,
    scale?: Vector,
    anchorX?: string,
    anchorY?: string, 
}